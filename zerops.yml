zerops:
    - setup: app
      build:
          base: nodejs@20
          buildCommands:
              - pnpm i
              - pnpm build
          deployFiles:
              - dist/recipe-angular-static/browser/~
      run:
          base: static
